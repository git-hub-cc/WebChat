version: '3'

services:
  app:
    image: openjdk:17
    container_name: my-app
    ports:
      - "8080:8080"
    volumes:
      - ./:/app
    working_dir: /app
    # 这里的包名可能需要修改
    command: java -jar app.jar